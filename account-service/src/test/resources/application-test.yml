# Test configuration
spring:
  datasource:
    url: jdbc:tc:postgresql:15:///account_test_db
    username: test
    password: test
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  data:
    redis:
      host: localhost
      port: 6379
  kafka:
    bootstrap-servers: localhost:9093

logging:
  level:
    com.minibank: DEBUG
